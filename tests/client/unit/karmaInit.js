describe('should run on karma', function () {
  it('karma will initially run correnctly', function (done) {
    expect(true).toEqual(true);
  });
  it('karma will fail', function (done) {
    expect(true).toEqual(true);
  });
});
